import PlaceholderScreen from '../../components/PlaceholderScreen';

// Not in the six mockup screens. Profile is out of scope for Sprint 1; the tab exists
// so navigation matches the mockup's tab bar.
export default function ProfileScreen() {
  return <PlaceholderScreen title="Perfil" subtitle="Fuera del alcance del Sprint 1" />;
}
